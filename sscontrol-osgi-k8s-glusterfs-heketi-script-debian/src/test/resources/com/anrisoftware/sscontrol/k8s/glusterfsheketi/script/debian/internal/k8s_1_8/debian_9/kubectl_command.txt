#!/bin/bash
theCommand="$0"
echo "$theCommand $@"
echo "$@" >> $theCommand.out

if [ "$#" -gt 0 ]; then
    cmd=($1)
    cmd=${cmd[0]}
    if [[ "$1" == "get" ]] && [[ "$2" == "sc" ]] && [[ "$3" == "slow" ]]; then
        exit 1
    fi
    if [[ "$1" == "get" ]] && [[ "$2" == "pvc" ]]; then
        echo "test      Bound     pvc-04632fa7-a81e-11e7-a96a-080027c56f49   11Gi      RWO       slow      44s"
    fi
    if command -v -- "$cmd"&>/dev/null; then
        if [[ "$cmd" != "enable" ]]; then
            $@
        fi
    fi
fi
