/*
 * vars.ports
 * vars.nodes
 */
ufwPrivatePorts(parent, vars) ::= <<
<vars.ports:allowPort(parent, vars);separator="\n">

>>

allowPort(port, parent, vars) ::= <<
<vars.nodes:{n|ufw allow from <n> to any port <port> proto tcp};separator="\n">
>>
