mirrorService(parent, vars) ::= <<
[Service]
Environment="DOCKER_OPTIONS=$DOCKER_OPTIONS <parent.service.registry.mirrorHosts:mirrorHosts(parent, vars);separator=" ">"

>>

mirrorHosts(host, parent, vars) ::= <%
--registry-mirror=<host.host>
%>
