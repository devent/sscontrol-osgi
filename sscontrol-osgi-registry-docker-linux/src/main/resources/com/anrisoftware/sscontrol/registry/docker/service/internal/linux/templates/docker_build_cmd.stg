dockerBuild(parent, vars) ::= <<
<setupDocker(parent, vars)>
docker build -t <dockerTag(parent, vars)>

>>

dockerTag(parent, vars) ::= <%
<if(vars.service.registry.address)>
<vars.service.registry.address>/
<endif>
registry/name:version
%>
